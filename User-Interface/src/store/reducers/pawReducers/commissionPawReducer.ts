
const initialState = {
    isPawCommissioning: false,
    message: undefined
};
export const commissionPaws = (state = initialState, action: any) => {
    switch(action.type) {
        default:
            return state;
    }
};
